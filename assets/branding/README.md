# Branding

`app_icon.png` — square artwork (1024² recommended), the single source for the
Android icon set. Regenerate everything with:

    python3 tool/make_branding.py

`logo.png` is generated from it and is what the app itself displays.
